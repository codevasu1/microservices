{
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon --watch . --watch config/.env.development app.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "nodemon": "^3.1.10"
  }
}
